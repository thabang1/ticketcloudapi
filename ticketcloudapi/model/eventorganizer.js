const mysql = require("mysql");

const db = mysql.createConnection({
    host    : "greenlinks1.dedicated.co.za",
    user    : "guest",
    password    : "password#123",
    database    : "ticketcloud"
});

db.connect(function(err){
    if(err){
        console.log(err);
    }else{
        console.log("connected to database test");
    }
});


exports.geteventorganizer = function(id, callback){
    let sql = `SELECT * From eventorganizer WHERE ideventorganizer = ?`;
    db.query(sql, [id], function(err, data){
        if(err){
            callback(err);
        }else{
            callback(null, data);
        }
    })
}



exports.inserteventorganizer = function(data, callback){
    
  let sql = "INSERT INTO eventorganizer SET ?";

    db.query(sql,[data], function(err, result){
        if(err){
            callback(err);
        }else{
            callback(null, result);
        }
    })
}



exports.updateeventorganizer = function(id, data, callback){
    let sql = "update eventorganizer set ? where ideventorganizer = ?";
    db.query(sql, [data, id], function(err, data){
        if(err){
            callback(err);
        }else{
            callback(null, data);
        }
    })
}


exports.deleteeventorganizer = function(id, callback){
    let sql = "DELETE from eventorganizer where ideventorganizer = ?";
    db.query(sql, [id],function(err, data){
        if(err){
            callback(err);
        }else{
            callback(null, data);
        }
    })
}